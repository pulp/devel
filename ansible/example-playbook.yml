---
- hosts: pulp3_dev
  become: true
  become_method: sudo
  vars:
    supplement_bashrc: true
  roles:
    - vagrant
    - common
    - db
    - broker
    - lazy
    - dev
    - plugins
    - reset_db
    - systemd
    # - pulp_ca
    # - smash
    # - pulpproject
    - dev_tools
    # - release
    # - debug
